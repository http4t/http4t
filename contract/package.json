{
  "name": "@http4t/contract",
  "version": "${VERSION}",
  "repository": "git@github.com:http4t/http4t.git",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc && madge --extensions ts --circular src/",
    "docs": "typedoc --includeDeclarations --excludeExternals --out dist/docs src",
    "test": "NODE_ENV=development ./node_modules/.bin/mocha --require ts-node/register --exit  'test/**/*.test.ts'",
    "test-browser": "ts-node test-browser.ts"
  },
  "devDependencies": {
    "typedoc": "^0.14.2",
    "typescript": "^3.3.3333",

    "ts-node": "^4.1.0",

    "@types/mocha": "^5.2.6",
    "mocha": "^5.2.0",
    "mocha-headless-chrome": "^1.8.2",

    "@types/chai": "^4.1.7",
    "chai": "^4.1.7",

    "madge": "^3.2.0",

    "@types/puppeteer": "^1.3.2",
    "fuse-box": "^3.1.3"
  },
  "optionalDependencies": {
    "@types/text-encoding": "^0.0.35",
    "@types/node": "^10.12.18",
    "text-encoding": "^0.6.4"
  }
}
