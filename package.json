{
  "name": "@http4t/root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "examples/**/src",
    "examples/**/test",
    "packages/**/src",
    "packages/**/test"
  ],
  "scripts": {
    "version": "ts-node build/version $BUILD_NUMBER write",
    "build": "ts-node build/build install",
    "test": "ts-node build/build",
    "clean": "ts-node build/clean"
  },
  "devDependencies": {
    "@types/node": "^10",
    "ts-node": "^8.10.2",
    "node": "^10.23.0",
    "typescript": "^4.1.2"
  }
}
